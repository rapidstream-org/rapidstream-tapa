"""The multi-eot-vadd test for TAPA."""

# Copyright (c) 2024 RapidStream Design Automation, Inc. and contributors.
# All rights reserved. The contributor(s) of this file has/have agreed to the
# RapidStream Contributor License Agreement.

load("//bazel:tapa_rules.bzl", "tapa_xo")
load("//bazel:v++_rules.bzl", "vpp_xclbin")

sh_test(
    name = "multi-eot-vadd",
    size = "medium",
    srcs = ["//bazel:v++_env.sh"],
    args = ["$(location multi-eot-vadd-host)"],
    data = [":multi-eot-vadd-host"],
    env = {"TAPA_CONCURRENCY": "1"},
)

sh_test(
    name = "multi-eot-vadd-xosim",
    size = "large",
    srcs = ["//bazel:v++_env.sh"],
    args = [
        "$(location multi-eot-vadd-host)",
        "--bitstream=$(location multi-eot-vadd-xo)",
        "--xosim_executable=$(location //tapa/cosim:tapa-fast-cosim)",
        "1000",
    ],
    data = [
        ":multi-eot-vadd-host",
        ":multi-eot-vadd-xo",
        "//tapa/cosim:tapa-fast-cosim",
    ],
    tags = [
        "cpu:2",
    ],
)

sh_test(
    name = "multi-eot-vadd-cosim",
    size = "large",
    srcs = ["//bazel:v++_env.sh"],
    args = [
        "$(location multi-eot-vadd-host)",
        "--bitstream=$(location multi-eot-vadd-hw-emu-xclbin)",
        "1000",
    ],
    data = [
        ":multi-eot-vadd-host",
        ":multi-eot-vadd-hw-emu-xclbin",
    ],
    tags = [
        "cpu:2",
    ],
)

cc_binary(
    name = "multi-eot-vadd-host",
    srcs = glob([
        "*.cpp",
        "*.h",
    ]),
    deps = [
        "//tapa-lib:tapa",
        "@gflags",
        "@vitis_hls//:include",
    ],
)

tapa_xo(
    name = "multi-eot-vadd-xo",
    src = "vadd.cpp",
    platform_name = "xilinx_u250_gen3x16_xdma_4_1_202210_1",
    top_name = "VecAdd",
)

vpp_xclbin(
    name = "multi-eot-vadd-hw-emu-xclbin",
    platform_name = "xilinx_u250_gen3x16_xdma_4_1_202210_1",
    target = "hw_emu",
    top_name = "VecAdd",
    xo = ":multi-eot-vadd-xo",
)
